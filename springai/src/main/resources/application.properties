# API key comes from the OPENAI_API_KEY environment variable (never commit keys).
# The default empty value lets the context start without a key; calls then fail
# until one is provided, and the tests skip themselves when it is absent.
spring.ai.openai.api-key=${OPENAI_API_KEY:}
spring.main.web-application-type=none
